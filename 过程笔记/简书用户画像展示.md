### 简书用户画像展示（利用Flask框架进行展示）

```python
#项目名称（jianshu_flask）

from flask import Flask, render_template

app = Flask(__name__)

# 一、单个用户的爱好
@app.route('/user', methods=['GET', 'POST'])
def user_hobby():
    return render_template('user_huaxiang.html')

# 单个用户的爱好展示页 根据用户的主页地址(user_homepage)进行数据收集、展示(饼图、词云图)
@app.route('/user/show', methods=['GET', 'POST'])
def user_hobby_show():
    pass


# 二、简书群体（女性、男性、未标注性别的 三类人群）的爱好排行榜
@app.route('/crowd', methods=['GET', 'POST'])
def Tophobby():
    return render_template('crowd_huaxiang.html')

# 展示 简书群体（女性、男性、未标注性别的 三类人群）的爱好排行榜；根据用户输入的 gender、user_num进行展示 (饼图)
@app.route('/crowd/show', methods=['GET', 'POST'])
def Tophobby_show():
    pass


# 三、数据库里所有的简书用户画像比较
@app.route('/huaxiang', methods=['GET', 'POST'])
def get_userhuaxiang():
    return render_template('huaxiang.html')

#根据前台输入的 user_num 进行展示
@app.route('/huaxiang/show', methods=['GET', 'POST'])
def userhuaxiang_show():
    pass


if __name__ == '__main__':
    app.run(debug=True)

```